import { UI } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
    console.log("Digitrikdoc Suite Professionale - Avvio applicazione");
    // L'inizializzazione della UI gestirà anche la data automatica
    UI.init();
});